
/**
 * @fileOverview This file previously configured the Genkit AI instance.
 * It is now deprecated as the project has shifted to a full simulation model
 * and no longer makes live calls to the Google AI API.
 * The 'ai' export is kept as null to ensure any remaining imports do not break the build,
 * but it should not be used.
 */

export const ai = null;
